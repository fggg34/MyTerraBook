/** Shared predictive city search settings for guesthouse booking bars. */
export const GUESTHOUSE_CITY_SEARCH_PROPS = {
  scope: 'guesthouse',
  allowFreeText: true,
  limit: 2,
  menuMode: 'inline',
  openOnFocus: false,
  maxMenuHeight: 112,
  closeOnViewportResize: true,
  suggestionTypes: ['city'],
}
